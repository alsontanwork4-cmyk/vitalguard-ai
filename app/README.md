Frontend stretch goal is intentionally deferred in this MVP.

Current submission-facing documents in this directory:

- `demo-artifacts.md`: judge-facing demo runbook, recovery demos, and backup artifact status.
- `submission-evidence-guide.md`: canonical claim matrix and form-answer wording guide.

Demo path for the current build:
- Use `python -m google.adk.cli web agents` for the built-in ADK UI during the hackathon demo.
- Optionally add a dashboard later under this directory if a future phase moves beyond the current submission-hardening scope.
