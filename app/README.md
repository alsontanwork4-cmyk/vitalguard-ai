Frontend stretch goal is intentionally deferred in this MVP.

Demo path for the current build:
- Use `adk web agents` for the built-in ADK UI during the hackathon demo.
- Optionally add a dashboard later under this directory if Phase 2 is stable.
